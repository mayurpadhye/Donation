<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools"
          package="com.example.myapplication">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
<uses-permission android:name="android.permission.CAMERA"></uses-permission>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
    <application
            android:allowBackup="false"
            android:icon="@drawable/logo"
            android:label="@string/app_name"
            tools:replace="android:allowBackup"
            android:roundIcon="@drawable/logo"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".donor.select_location.view.SelectLocationActivity">
        </activity>
        <activity android:name=".benificiary.home.BenificiaryHomeActivity">
        </activity>
        <activity android:name=".benificiary.registration.view.RegistrationActivity">
        </activity>
        <activity android:name=".donor.benificiary_list.view.BenificiaryListActivity">
        </activity>
        <activity android:name=".benificiary.login.view.LoginActivity">
        </activity>
        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".donor.home.view.HomeActivity">
        </activity>
        <activity
                android:name=".ChooseRoleActivity"
                android:screenOrientation="portrait"
                android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>